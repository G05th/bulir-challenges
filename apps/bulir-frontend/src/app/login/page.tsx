import LoginForm from '../../components/LoginForm';

export default function LoginPage() {
  return (
    <main className="page flex items-center justify-center pt-16">
      <LoginForm />
    </main>
  );
}
