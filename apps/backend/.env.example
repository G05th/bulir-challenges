# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="file:./dev.db"

################################################################
# CONFIGURAÇÕES DA APLICAÇÃO GERAL
################################################################

# Porta em que o NestJS será executado (Padrão: 3000)
PORT=3000

# Ambiente de execução (development | production)
NODE_ENV=development

################################################################
# CONFIGURAÇÕES DE AUTENTICAÇÃO (JWT)
################################################################

# A chave secreta usada para assinar e verificar os tokens JWT.
# ESSA CHAVE DEVE SER LONGA, COMPLEXA E MANTIDA EM SEGREDO.
# Use a mesma chave que você configurou em seu JwtModule e JwtStrategy.
JWT_SECRET=sua_chave_secreta_muito_forte_e_aleatoria_aqui_1234567890

# Tempo de expiração do token (Ex: 60m, 1h, 7d)
JWT_EXPIRES_IN=60m

################################################################
# CONFIGURAÇÕES DO BANCO DE DADOS (PRISMA)
################################################################

# Database URL: A string de conexão do seu banco de dados.
# Exemplos:
# SQLite (Para desenvolvimento): file:./dev.db
# PostgreSQL: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# MySQL: mysql://USER:PASSWORD@HOST:PORT/DATABASE
